# i18n (fordítás)

Greenfield: [uj-projekt.md](uj-projekt.md) → i18n lépés.  
Admin kinézet/működés: [admin-oldal.md](admin-oldal.md).  
Éles playbook: [minta-tanulsagok.md](minta-tanulsagok.md) §0.2.

## Szabály (minden prefix)

1. **Minden** látható / statikus UI szöveg `__()` (vagy `__d()`) hívásban legyen — Admin, Member, közös layout/element, Flash, hibaoldalak.
2. A **msgid angol** (fordítási forrásnyelv), pl. `__('Save')`, `__('Samples')`.
3. A megjelenő nyelv a **locale** szerint jön a `.po` fájlokból.
4. JS nem tud `__()`-t: a lefordított stringeket PHP adja át (`MyAdmin.messages` a layoutban; oldalspecifikus címkék a `scriptBlock` configban `json_encode` + `__()`).
5. Admin böngésző dialógusok: **SweetAlert2** (`MyAdmin.alert` / `alertError` / `confirmDelete`); a Swal szövegek is a `MyAdmin.messages`-ből jöjjenek (`errorTitle`, `okButton`, …) — ne hardkódolt magyar a JS-ben, és **ne** `window.alert`.
6. **Számok megjelenítése** (index/view/modal): `LocaleNumberParser::format()` az aktuális locale szerint (Admin hu → `1 234,56`). A `__()` csak címkékre — msgid angol.
7. **Pénz megjelenítés:** `LocaleNumberParser::formatCurrency()` — mindig **HUF**, ICU locale szerint (hu: `… Ft` utótag; en: `HUF …` előtag; de/fr/sk: `… HUF`). **Nem** `__('Ft')` / kézi `format()`+szuffixum. Más valuta: `$currency` param.
8. Soha ne hardkódolj magyar (vagy más célnyelvű) UI stringet a templatebe „gyorsabbnak” — mindig `__()` + .po. Kivétel a locale formázók (`format`, `formatCount`, `formatCurrency`).

### Gyakori Admin msgid-ek (lapozó / kereső)

| Msgid | hu_HU példa |
|-------|-------------|
| `Back to top` | Vissza a tetejére |
| `Pagination` | Lapozás |
| `First` / `Previous` / `Next` / `Last` | Első / Előző / Következő / Utolsó |
| `Page {{page}} of {{pages}}, showing {{current}} record(s) out of {{count}} total` | bake counter (placeholderek maradnak) |
| `Start search` / `Clear search` | Keresés indítása / Keresés törlése |
| `Search in the text fields of this list.` | Index tooltip magyarázat |
| `Search in the text fields of all configured tables.` | Globális tooltip |
| `Profile` / `Change password` / `Current password` / `New password` | Profil / Jelszó módosítása / … |
| `Back to profile` / `Back to admin` / `Country` | auth UI — [users-auth.md](users-auth.md) |
| `Setups` / `Settings` | Beállítások / Konfiguráció |
| `New setup` / `Edit setup` / `Setup details` | Új / Szerkesztés / Részletek |
| `Slug` / `Type` / `Value` / `Description` | mezőcímkék |
| `e.g. site_title` | pl. site_title |
| `Lowercase letters, numbers and underscores only. Suggested from the name.` | form súgó |
| `The slug may only contain lowercase letters, numbers and underscores.` | validáció |
| `This slug is already in use.` | unique |
| `String` / `Text` / `Integer` / `Float` / `Boolean` / `JSON` / `Array` | típuscímkék |
| `Enter a JSON object or array.` / `One value per line (or a JSON array).` | value súgók |
| `Only visible countries` | Csak a látható országok (Countries index szűrő) |
| `Number of users` | Felhasználók száma (Countries `user_count` oszlop) |
| `Locale` | Nyelvi kód (Countries index oszlop; ne maradjon angol) |
| `Unsaved changes` / `Leave` / `Stay` | Mentetlen form leave Swal |
| `You have unsaved changes. Do you really want to leave this form?` | Mentetlen form szöveg |

9. Dátum picker / szám inputmask formátum: `LocaleDateParser::jsConfig()` / `LocaleNumberParser::jsConfig()` — követi az Admin locale-t ([middleware.md](middleware.md)).

## Locale

| Kontextus | Locale |
|-----------|--------|
| App default | `hu_HU` (`config/app.php` → `App.defaultLocale`) |
| **Admin / New / Member / … panelek** | `BrowserLocale::forLoggedIn` (login session/cookie, majd user ország); nincs URL `{lang}` |
| Auth / lang nélkül (`/login`, …) | session → cookie `AppUiLocale` (≥1 év) → böngésző → `countries.locale` — [users-auth.md](users-auth.md) |
| **Bejelentkezés után** | locale mint fent; **cél URL** = `RoleHome` (`new` → `/new`, …) |

Beállítás (egy helyen):

```php
// config/app.php
'adminLocale' => env('APP_ADMIN_LOCALE', 'hu_HU'),
```

Használat:

- `LocaleMiddleware` — session/cookie (nincs URL lang)
- Auth Flash: Simple Notify (login layout) — [users-auth.md](users-auth.md)
- Panel / Admin AppController — `BrowserLocale::forLoggedIn`

Ideiglenes EN UI/teszt: `en_US` (vagy `.env`). **Éles indulás előtt vissza `hu_HU`.**

Adminban **nincs** nyelvválasztó a headerben.

## Fájlok

```
resources/locales/default.pot          # extract forrás (bin/cake i18n extract)
resources/locales/hu_HU/default.po     # magyar (teljes Admin + auth)
resources/locales/{locale}/default.po  # auth UI: minden `languages.visible=true` kód (pl. de_DE, fr_FR, en_US, …)
config/languages.php                   # URL lang → locale (Member)
config/schema/i18n.sql                 # DB Translate EAV tábla
config/schema/countries.sql            # Országok + primary locale
```

**Auth országváltás:** a regisztrációnál választott ország `locale`-ja → `I18n::setLocale` + cookie `AppUiLocale` (≥1 év) → `resources/locales/{locale}/default.po`.  
**Login után:** a login képernyőn választott nyelv (session/cookie) megy a teljes Admin UI-ra; ha nincs, fallback a `users.country_id` locale.  
Újraépítés (`languages.visible = true` listája alapján): `php tmp/build_auth_locale_pos.php`  
(kiegészítő fordítások: `tmp/auth_locale_extra.php`, `tmp/auth_locale_new_langs*.php`)

Új szövegek felvétele:

```bash
bin/cake i18n extract --extract-core no --overwrite --merge yes --output resources/locales --paths src,templates
# majd egészítsd ki a hu_HU/default.po msgstr értékeit
# segéd: php tmp/build_hu_po.php  (ha a map naprakész)
```

Szerepkör-megnevezések (`App\Auth\AppRoles::label()`): msgid angol (`Superuser`, `Admin`, `President`, …); megjelenés `AppRoles::labeled()` → `admin — Admin`. Fordítás: `resources/locales/default.pot` + locale `.po`.

## Országnevek (DB Translate)

UI stringek ≠ adatbázis tartalom. Az **országnevek** a `countries` + `i18n` táblákban vannak (nem `.po`).

| Tábla / mező | Tartalom |
|--------------|----------|
| `countries.name` | Angol kanonikus név (pl. `Hungary`) |
| `countries.iso2` | ISO 3166-1 alpha-2 (`HU`) |
| `countries.locale` | Az ország **elsődleges** locale-ja (`hu_HU`, `de_DE`, `en_US`, …) |
| `continents` | Földrészek: angol `name` + `code` (AFR/ASI/…) |
| `countries.continent_id` | FK → `continents.id` (CakePHP: `belongsTo Continents`) |
| `i18n` (Continents) | Translate EAV: `model=Continents`, `field=name` — CLDR földrésznévek **minden** Countries-locale-ra; Admin UI locale szerint jelenik meg |
| `countries.visible` / `pos` | Admin: **admin** csak ezeket szerkeszti; **superuser** teljes mezők + add/delete |
| `i18n` (Countries) | Translate EAV: `model=Countries`, `field=name` — **csak megjelenítés** (UI locale); Admin formon **nincs** fordítás-szerkesztő; **index/modal soha ne sorolja fel** az i18n sorokat |

Admin: `/admin/countries` — lista / view / edit (+ superuser: add/delete).  
Lista UI (szűrő, oszlopsorrend, szélességek): **[countries-admin.md](countries-admin.md)**.  
Lista később **continent** szerint csoportosítható.

### Form nyelvi TAB-ok (Samples / Parents / …)

Teljes playbook: **[form-i18n-tabs.md](form-i18n-tabs.md)**; rule: `admin-form-i18n-tabs.mdc`.

Element: `templates/element/admin/form_language_fields.php`  
Tabs: `FormLanguages::tabs()` — látható országok, nyelv-kód szerint dedup (EN első).

| UI | Viselkedés |
|----|------------|
| Fül felirat | rövid kód (`EN`, `HU`, …) |
| Tooltip | ország neve az **UI locale** szerint + ISO — pl. hu oldalon „Magyarország (HU)” |
| Tooltip technika | `data-bs-toggle="tooltip"` a felirat `<span>`-jén (a gomb marad `tab`) |
| Default locale mezők | entity root (`name`); egyéb: `_translations.{locale}.*` |
| Cake 5.3 | `getBehavior('Translate')->setLocale()` / `translationField()` — ne Table proxy |
| `error` opció | soha bool `true`; más fülön `false` |

`MyAdmin.initTooltips()` a layoutból inicializálja.

### Index keresés / rendezés — Translate (kötelező)

A megjelenített `name` (és egyéb Translate mezők) UI locale szerint jönnek; a **keresés és a sort is** ugyanazon a locale-on fusson — ne a kanonikus angol oszlopon.

| Réteg | Viselkedés |
|-------|------------|
| Locale | `AdminCountry::normalizeTranslateLocale(I18n::getLocale())` → minden Translate tábla `setLocale` |
| Keresés | `AdminSearch::applyToQuery` → `translationField()` LIKE + kanonikus fallback |
| Rendezés | `AppController::indexPaginateOptionsFor($table, …)` → `sortableFields` map: `Alias_field_translation.content`, majd `Alias.field` (ne `COALESCE` — a Paginator `_prefix` elrontja) |
| Nincs Translate | Cities, Setups — változatlan `{Alias}.{field}` |

Új index controller:

```php
$paginateOptions = $this->indexPaginateOptionsFor($this->Items, [
    'sortableFields' => ['id', 'name', 'Parents.name', …],
    'order' => ['name' => 'ASC'], // opcionális
], [
    'Parents' => $this->Items->Parents->getTarget(), // ha belongsTo is Translate
]);
$query = $this->applyIndexSearch($this->Items->find()->contain(['Parents']), $this->Items);
```

API: `src/Utility/AdminTranslate.php`. Ne írj kézi `Alias.name LIKE` / `orderBy(['Alias.name'=>…])` Translate mezőkre.  
Rule: `.cursor/rules/admin-translate-search-sort.mdc`  
**Tilos:** `COALESCE(…)` mint ORDER BY kulcs (Paginator `_prefix` SQL hibát okoz).

```php
$countries = $this->fetchTable('Countries');
// vagy: AdminCountry::applyTranslateLocale(); / find('visibleTranslated')
$countries->getBehavior('Translate')->setLocale(
    \App\Utility\AdminCountry::normalizeTranslateLocale(I18n::getLocale())
);
$countries->Continents->getBehavior('Translate')->setLocale(
    \App\Utility\AdminCountry::normalizeTranslateLocale(I18n::getLocale())
);
$hu = $countries->find()->contain(['Continents'])->where(['Countries.iso2' => 'HU'])->first();
// hu_HU → Magyarország + Európa; de_DE → Ungarn + Europa; en_* → Hungary + Europe

// Select2 / csak látható országok, UI nyelv szerinti név:
$options = \App\Utility\AdminCountry::options(); // id => "Magyarország (HU)"
```

Seed:

```bash
php tmp/seed_continents.php   # continents + i18n + countries.continent_id
# (országok: php tmp/seed_countries.php ha van)
```

### Árva i18n sorok tisztítása

A Translate EAV polimorf (`model` + `foreign_key`) — nincs DB FK. Törölt szülő után maradhat árva sor.

```bash
bin/cake cleanup_i18n_orphans           # törli az árvákat
bin/cake cleanup_i18n_orphans --dry-run # csak számol
# vagy: php tmp/cleanup_i18n_orphans.php
```

| `i18n.model` | Szülő tábla |
|--------------|-------------|
| `Countries` | `countries` |
| `Continents` | `continents` |

Ismeretlen `model` érték → az összes ilyen sor törlődik. Árva = `foreign_key` nincs a szülő `id`-k között.

### Index lapozó counter (bake)

Az Admin index lábléc (`admin/index_footer`) a Cake bake formátumot használja — a **msgid angol**, a placeholderek benne maradnak (a Paginator cseréli ki fordítás után):

```php
$this->Paginator->counter(
    __('Page {{page}} of {{pages}}, showing {{current}} record(s) out of {{count}} total')
);
```

`.po` példa (hu):

```
msgid "Page {{page}} of {{pages}}, showing {{current}} record(s) out of {{count}} total"
msgstr "{{page}}. oldal / {{pages}}, megjelenítve: {{current}} rekord, összesen: {{count}}"
```

Ha a `countries.id` értékeket újra kell számozni (pl. 1-től):

```bash
php tmp/renumber_countries_ids.php
```

A script az `i18n` (`model=Countries`) `foreign_key` mezőit **ugyanazzal az offsettel** frissíti, hogy a fordítások ne szakadjanak el.

## Példák

```php
<?= __('Save') ?>
$this->Flash->success(__('The sample has been saved.'));
$this->Paginator->sort('name', __('Name'));
```

```php
// Layout: MyAdmin.messages
'deleteConfirm' => __('Do you really want to delete the selected record?'),
```
