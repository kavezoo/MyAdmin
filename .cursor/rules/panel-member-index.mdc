---
description: Role panel taglista index — Admin index playbook (sort, modal, parent link)
globs: templates/**/Members/index.php,templates/**/Applicants/index.php
alwaysApply: false
---

# Role panel — taglista index (kötelező minta)

Ha **President** / **Clubpresident** (vagy más panel) **Users** alapú listát mutat, **ne** egyszerű táblát másolj — ugyanaz az Admin `index.php` playbook:

## Kötelező (checklist)

1. **Fájl teteje** — `$rowDoubleClickAction`, opcionális oszlop kapcsolók (`$showIdColumn`, `$showCreatedColumn`, …), `$indexColspan`, `MyAdmin.config` (`recordGetUrl`, `viewUrl`, …).
2. **Tábla** — `class="index-data-table"`; **minden** sortolható oszlop: `$this->Paginator->sort(...)`.
3. **Szülő rekord** (pl. `Clubs` a taglistán) — **ID után első oszlop**: `category-link` + AJAX → `#modalLinkedRecordView` (`clubRecordGet` / `parentGet` minta).
4. **Logikai mezők** — `active`, `enabled`: zöld pipa / piros X (nem nyers 0/1).
5. **Modal** — `admin/modal_record_view` + kapcsolt esetén `modal_linked_record_view`; `recordGet` JSON `can_delete: false` ha nincs törlés.
6. **Actions** — view + edit + delete gombok; törlés `btn-secondary disabled` ha tiltott.
7. **Controller** — `sortableFields` a `paginate()`-ben; `recordGet` (+ szülő `clubRecordGet`); ACL scope (ország / klub).
8. **JS** — `pages/index.js` + domain script (pl. tagdíj SWAL); ne hagyd ki a modalt.

Referencia: `templates/President/Members/index.php`, `templates/Admin/Samples/index.php`, `src/Controller/Concerns/PanelMemberListTrait.php`.

Spec: [admin-konvenciok.md](admin-konvenciok.md) (Index lista), [membership.md](membership.md) § Taglisták.
